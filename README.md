# Liquid Glass Clock

Transparent, frameless, always-on-top Electron desktop clock built with React, TypeScript, and `solid-glass`.

## Scripts

```bash
npm run dev
npm run build
```

`npm run build` emits a portable Windows executable in `release/` instead of an installer.
